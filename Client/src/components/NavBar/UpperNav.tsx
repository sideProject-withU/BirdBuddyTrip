import styled from 'styled-components';

const Container = styled.nav`
  height: 5rem;
  background-color: rgb(196, 196, 196);
`;

const UpperNav = () => {
  return <Container>높이보기</Container>;
};

export default UpperNav;
